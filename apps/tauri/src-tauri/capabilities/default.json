{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: grant core IPC (invoke) and event (listen/emit) access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}